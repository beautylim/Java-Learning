<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.shop.mapper.FollowMapper">

    <insert id="insert" parameterType="org.example.shop.model.entity.Follow">
        INSERT INTO tb_follow (user_id, follow_user_id)
        VALUES (#{userId}, #{followUserId});
    </insert>
    <delete id="delete">
        delete from tb_follow where user_id = #{userId} and follow_user_id = #{followUserId}
    </delete>
    <select id="getCount" resultType="java.lang.Integer">
        select count(*) from tb_follow where user_id = #{userId} and follow_user_id = #{followUserId}
    </select>
    <select id="getAllUserId" resultType="java.lang.Long" parameterType="java.lang.Long">
        select user_id from tb_follow where follow_user_id = #{followUserId}
    </select>
</mapper>