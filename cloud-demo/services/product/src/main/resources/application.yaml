server:
  port: 8070
  address: 0.0.0.0

spring:
  application:
    name: product
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        username: nacos
        password: nacos
        register-enabled: true  # 开启自动注册（默认就是true）